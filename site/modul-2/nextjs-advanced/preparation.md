# Preparation

No new app this time — keep using the one from [Course 5's Preparation](modul-2/nextjs/preparation.md).

```bash
npm run dev
```

`"use client"` and `"use server"` are both built into Next.js since version 13.4, and stable — no experimental flag needed. One real package for this course, used in the `server-only` chapter:

```bash
npm install server-only
```
